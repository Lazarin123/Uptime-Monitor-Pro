# Porta em que a API e o WebSocket vão escutar
PORT=4000

# Intervalo padrão (em segundos) entre checagens, usado quando o serviço
# não define um intervalo próprio
DEFAULT_CHECK_INTERVAL_SECONDS=15

# Origem permitida para CORS (URL do frontend em dev)
CORS_ORIGIN=http://localhost:5173
